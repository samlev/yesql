<x-slides.title-and-body title="How does it work?">
  <div x-transition x-show="$wire.slide === 0">
    <p>Create a table with a binary column</p>
  </div>
  <div x-transition x-show="$wire.slide === 1">
    <p>Create a table with a binary column</p>
  </div>
  <div x-transition x-show="$wire.slide === 2">
    <p>Create a table with a binary column</p>
  </div>
  <div x-transition x-show="$wire.slide === 3">
    <p>Create a table with a binary column</p>
  </div>
  <div x-transition x-show="$wire.slide === 4">
    <p>Create a table with a binary column</p>
  </div>
  <div x-transition x-show="$wire.slide === 5">
    <p>Create a table with a binary column</p>
  </div>
</x-slides.title-and-body>
